-- CreateTable
CREATE TABLE "SCHEDULING" (
    "id" TEXT NOT NULL,
    "hospitalId" TEXT NOT NULL,
    "createdById" TEXT NOT NULL,
    "providerId" TEXT NOT NULL,
    "patientId" TEXT NOT NULL,
    "observation" TEXT,
    "startDate" TIMESTAMP(3) NOT NULL,
    "endDate" TIMESTAMP(3) NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "SCHEDULING_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE INDEX "SCHEDULING_patientId_idx" ON "SCHEDULING"("patientId");

-- CreateIndex
CREATE INDEX "SCHEDULING_createdById_idx" ON "SCHEDULING"("createdById");

-- CreateIndex
CREATE INDEX "SCHEDULING_providerId_idx" ON "SCHEDULING"("providerId");

-- CreateIndex
CREATE INDEX "SCHEDULING_hospitalId_idx" ON "SCHEDULING"("hospitalId");

-- AddForeignKey
ALTER TABLE "SCHEDULING" ADD CONSTRAINT "SCHEDULING_hospitalId_fkey" FOREIGN KEY ("hospitalId") REFERENCES "HOSPITAL"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "SCHEDULING" ADD CONSTRAINT "SCHEDULING_createdById_fkey" FOREIGN KEY ("createdById") REFERENCES "USER"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "SCHEDULING" ADD CONSTRAINT "SCHEDULING_providerId_fkey" FOREIGN KEY ("providerId") REFERENCES "USER"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "SCHEDULING" ADD CONSTRAINT "SCHEDULING_patientId_fkey" FOREIGN KEY ("patientId") REFERENCES "PATIENT"("id") ON DELETE RESTRICT ON UPDATE CASCADE;
