-- CreateTable
CREATE TABLE "ARCHIVE" (
    "id" TEXT NOT NULL,
    "url" TEXT NOT NULL,
    "name" TEXT NOT NULL,
    "type" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "messageId" TEXT,

    CONSTRAINT "ARCHIVE_pkey" PRIMARY KEY ("id")
);

-- CreateTable
CREATE TABLE "EXAM" (
    "id" TEXT NOT NULL,
    "hospitalId" TEXT NOT NULL,
    "createdById" TEXT NOT NULL,
    "providerId" TEXT NOT NULL,
    "patientId" TEXT NOT NULL,
    "archiveId" TEXT,
    "description" TEXT NOT NULL,
    "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP,
    "updatedAt" TIMESTAMP(3) NOT NULL,

    CONSTRAINT "EXAM_pkey" PRIMARY KEY ("id")
);

-- CreateIndex
CREATE UNIQUE INDEX "EXAM_archiveId_key" ON "EXAM"("archiveId");

-- AddForeignKey
ALTER TABLE "ARCHIVE" ADD CONSTRAINT "ARCHIVE_messageId_fkey" FOREIGN KEY ("messageId") REFERENCES "MESSAGE"("id") ON DELETE SET NULL ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "EXAM" ADD CONSTRAINT "EXAM_hospitalId_fkey" FOREIGN KEY ("hospitalId") REFERENCES "HOSPITAL"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "EXAM" ADD CONSTRAINT "EXAM_createdById_fkey" FOREIGN KEY ("createdById") REFERENCES "USER"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "EXAM" ADD CONSTRAINT "EXAM_providerId_fkey" FOREIGN KEY ("providerId") REFERENCES "USER"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "EXAM" ADD CONSTRAINT "EXAM_patientId_fkey" FOREIGN KEY ("patientId") REFERENCES "PATIENT"("id") ON DELETE RESTRICT ON UPDATE CASCADE;

-- AddForeignKey
ALTER TABLE "EXAM" ADD CONSTRAINT "EXAM_archiveId_fkey" FOREIGN KEY ("archiveId") REFERENCES "ARCHIVE"("id") ON DELETE SET NULL ON UPDATE CASCADE;
